import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles(() => ({
  container: {
    width: 1000,
    backgroundColor: "black",
    borderColor: "white",
    marginTop: 60,
  },
}));