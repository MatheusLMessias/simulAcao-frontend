import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles(() => ({
  title: {
    color:"white",
    fontSize: 32,
    marginBottom: -40,
  },
  subTitle: {
    color:"white",
    fontSize: 18,
    marginBottom: 40
  },
}));
